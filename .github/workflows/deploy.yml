name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build and restart
        run: |
          cd /home/ubuntu/docscanner
          git pull origin main
          docker compose build docscanner
          docker compose up -d docscanner
